//Funcion en el contexto
// export const getHeroByName = (name = "", heroes) => {
//   name = name.toLowerCase().trim();

//   if (name.length === 0) return [];

//   return heroes.filter((hero) => hero.name.toLowerCase().includes(name));
// };
